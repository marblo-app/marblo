schema_version: 1

id: superpowers-using-git-worktrees
name: Using Git Worktrees (Superpowers)
type: skill
version: 6.2.0
status: active
description: Ensures work happens in an isolated workspace — detect existing isolation first (with a submodule guard), prefer the harness's native worktree tooling, fall back to plain git worktrees only when there is none. Referenced by manifest; not vendored.

i18n:
  ko:
    name: "Git 워크트리 사용 (Superpowers)"
    description: "작업이 격리된 워크스페이스에서 이뤄지도록 보장 — 기존 격리를 먼저 감지(서브모듈 가드 포함), 하네스 네이티브 워크트리 도구를 우선 사용, 없을 때만 순수 git worktree로 폴백. 매니페스트로 참조되며 벤더링되지 않음."

publisher:
  name: Jesse Vincent (obra)
  tier: community
  url: https://github.com/obra

source:
  repository: https://github.com/obra/superpowers
  ref: v6.2.0
  path: skills/using-git-worktrees

compatibility:
  harnesses:
    - claude-code
    - codex
    - any

# Not vendored: the app copies these files from the pinned `source` ref above,
# checking every byte against the digests below before anything lands on disk.
# Generated by scripts/gen-community-installs.mjs — regenerate, don't hand-edit.
install:
  kind: files
  root: claude-skills
  dest: superpowers-using-git-worktrees
  files:
    - SKILL.md
  integrity:
    algorithm: sha256
    files:
      SKILL.md: 8cfb86f121269e8f7f12361e6795c4f6738828340e28964c9229d365666c9edd

permissions:
  - repository:read
  - repository:write
  - filesystem:write
  - shell:exec

keywords:
  - worktree
  - isolation
  - git
  - parallel-agents
  - external

license: MIT
homepage: https://github.com/obra/superpowers/tree/main/skills/using-git-worktrees
