schema_version: 1

id: mongodb-mcp
name: MongoDB MCP
type: mcp-server
version: 1.14.0
status: active
description: MongoDB's official server — explore collections, run queries and aggregations, inspect indexes, and drive Atlas cluster administration, with a --readOnly flag that disables every mutating tool. Referenced by manifest; not vendored.

i18n:
  ko:
    description: "MongoDB 공식 서버 — 컬렉션 탐색, 쿼리·집계 실행, 인덱스 점검, Atlas 클러스터 관리까지, 모든 변경 툴을 비활성화하는 --readOnly 플래그 제공. 매니페스트로 참조되며 벤더링되지 않음."

publisher:
  name: MongoDB
  tier: community
  url: https://github.com/mongodb-js

# Manifest-only: the real code stays upstream, pinned to a released tag.
# Newer `v1.15.0-alpha.*` tags exist; the pin tracks the latest stable release.
source:
  repository: https://github.com/mongodb-js/mongodb-mcp-server
  ref: v1.14.0

compatibility:
  harnesses:
    - claude-code
    - codex
    - any

permissions:
  - network:outbound
  - secrets:read

keywords:
  - mongodb
  - atlas
  - database
  - nosql
  - external

license: Apache-2.0
homepage: https://github.com/mongodb-js/mongodb-mcp-server
